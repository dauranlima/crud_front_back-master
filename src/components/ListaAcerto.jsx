import CartContext from "@/context/CartContext";
import { useContext } from "react"

export default function ListaAcerto() {
  const {editPedidos,acertos} = useContext(CartContext);
  return (
    <div>
      
    </div>
  )
}